# experiments

Place experiment harnesses and CSV/JSON outputs here. Each experiment should be reproducible via a small runner script that documents parameters and datasets used.

Suggested files:
- `run_experiments.sh` : orchestrates a sequence of runs and writes results into `experiments/results/`.
- `results/` : store CSV/JSON outputs and plots.
