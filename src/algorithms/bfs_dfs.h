// bfs_dfs.h
//
// Breadth-first search (BFS) and Depth-first search (DFS) implementations.
// Used for:
// - reachability checks from a source node
// - traversal of the resistance graph
// - connected component discovery
// - visualization of spread patterns
//
// Interface to add later:
// - bfs(graph, source) -> vector of reachable nodes
// - dfs(graph, source) -> vector of visited nodes in DFS order
// - reachable_count(graph, source) -> int
