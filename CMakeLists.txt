# Minimal CMake placeholder for building the C++ graph engine.
# Populate with project name, required C++ version, and targets later.
# Example steps:
# - project(TraceNet)
# - set(CMAKE_CXX_STANDARD 17)
# - add_executable(tracenet src/main.cpp src/graph.cpp)
