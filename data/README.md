# data

Place raw and processed input datasets here.
- `raw/` : original curated datasets (do not modify)
- `processed/` : outputs from preprocessing, including adjacency files consumed by the C++ engine

Do NOT commit large raw files to the repo; use a separate data storage solution and add download scripts if needed.
